<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="light" />
    <title>Custom Email Application</title>
    <style>
      body {
        background-color: #f4f4f4;
        margin: 0;
        padding: 20px;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Helvetica, Arial, sans-serif;
      }
      .email-container {
        max-width: 600px;
        background-color: #ffffff;
        margin: 0 auto;
        padding: 24px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }
      p {
        line-height: 1.6;
        margin-bottom: 16px;
        color: #333;
      }
      a {
        color: #1f3a86;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      @media only screen and (max-width: 600px) {
        .email-container {
          padding: 16px;
        }
      }
    </style>
  </head>
  <body>
    <!-- This hidden section provides a fallback preview text -->
    <div style="display: none; max-height: 0; overflow: hidden">
      I wish to apply for the position of <%= jobTitle %>.
    </div>
    <div class="email-container">
      <div>
        <!-- Custom greeting line without the extra comma -->
        <p><%= customGreeting %> <span style="color: #2563eb"><%= name %></span>,</p>
        <!-- Custom body paragraphs -->
        <% if (paragraphs && paragraphs.length > 0) { %>
          <% paragraphs.forEach(function(para) { %>
            <p><%= para %></p>
          <% }); %>
        <% } %>
        <!-- Custom closing line -->
        <p><%= customClosing %>,<br /><span style="color: #2563eb"><%= yourName %></span></p>
      </div>
    </div>
  </body>
</html> 